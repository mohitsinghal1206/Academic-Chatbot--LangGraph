# Academic-Chatbot--LangGraph
Conditional RAG chatbot for college students, built with LangGraph. Routes queries (academic / fee / general) through a classifier node to the right retriever, then generates a programme-personalized answer using Groq LLM + FAISS + HuggingFace embeddings. Streamlit UI included.
